const initialState = {
    credentials: {
        username: '',
        password: '',
        authType: ''
    }
}

export const reducer = (state = initialState, action) => {
    switch(action.type){
        
        default:
            return state;
    }
}